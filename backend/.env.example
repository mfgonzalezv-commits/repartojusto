# Server
PORT=3000
NODE_ENV=development

# Database PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/repartojusto

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=cambia_este_secreto_por_algo_muy_seguro_en_produccion
JWT_EXPIRES_IN=7d

# Google Maps API (mock por ahora)
GOOGLE_MAPS_API_KEY=mock_google_maps_key

# Flow API (mock por ahora)
FLOW_API_KEY=mock_flow_key
FLOW_SECRET=mock_flow_secret
FLOW_ENVIRONMENT=sandbox

# Firebase FCM (mock por ahora)
FIREBASE_PROJECT_ID=mock_firebase_project
FIREBASE_PRIVATE_KEY=mock_private_key
FIREBASE_CLIENT_EMAIL=mock_client_email

# App config
APP_FEE=500
RIDER_TARIFA_KM=650
RESIDUAL_PCT=8

# Admin (para seed)
ADMIN_EMAIL=admin@repartojusto.cl
ADMIN_PASSWORD=admin123